
let board;

function init(){
    board = [
        '','','',
        '','','',
        '','',''
    ];
    render();
};

init();

function render() {
    board.forEach(functiom(mark, index){
        console.log(mark, index);
    });
};
